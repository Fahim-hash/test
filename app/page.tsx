'use client';

import { useMemo, useState } from 'react';
import './kinetix.css';

type CardKind = 'LINK' | 'BOOST' | 'BREAK' | 'REVERSE CURRENT' | 'WILD';
type Card = { id: number; kind: CardKind; color: string; value: string };
type Player = { name: string; hand: Card[]; score: number };

const COLORS = ['red', 'blue', 'violet', 'gold'];
const KINDS: CardKind[] = ['LINK', 'BOOST', 'BREAK', 'REVERSE CURRENT', 'WILD'];
const glyph: Record<CardKind, string> = { LINK: '◈', BOOST: '↗', BREAK: '×', 'REVERSE CURRENT': '⟲', WILD: '✦' };
const subtitles: Record<CardKind, string> = { LINK: 'CONNECT', BOOST: 'ACCELERATE', BREAK: 'DISRUPT', 'REVERSE CURRENT': 'FLIP THE FLOW', WILD: 'ANY CURRENT' };

function buildDeck() { const deck: Card[] = []; let id = 1; for (let round = 0; round < 5; round++) for (const color of COLORS) for (const kind of KINDS) deck.push({ id: id++, kind, color, value: kind === 'REVERSE CURRENT' ? '↔' : kind === 'WILD' ? '✦' : String(round + 1) }); return deck.sort(() => Math.random() - 0.5); }
function playable(card: Card, top?: Card) { if (!top) return true; return card.kind === 'WILD' || card.color === top.color || card.kind === top.kind || card.value === top.value; }

function CardVisual({ card, selected, onClick, disabled = false, large = false }: { card: Card; selected?: boolean; onClick?: () => void; disabled?: boolean; large?: boolean }) {
  return <button disabled={disabled} onClick={onClick} className={`kin-card ${card.color} ${selected ? 'selected' : ''} ${disabled ? 'disabled' : ''} ${large ? 'large' : ''}`} aria-label={`${card.color} ${card.kind} ${card.value}`}>
    <span className="card-frame" /><span className="card-top"><b>{card.value}</b><small>KX / {card.kind}</small></span>
    <span className="card-art" aria-hidden="true"><i className="art-ring ring-a" /><i className="art-ring ring-b" /><i className="art-line line-a" /><i className="art-line line-b" /><strong>{glyph[card.kind]}</strong></span>
    <span className="card-title">{card.kind}</span><span className="card-subtitle">{subtitles[card.kind]}</span><span className="card-corner bottom">{card.value}</span>
  </button>;
}

export default function Home() {
  const [players, setPlayers] = useState<Player[]>([]); const [deck, setDeck] = useState<Card[]>([]); const [discard, setDiscard] = useState<Card[]>([]); const [current, setCurrent] = useState(0); const [direction, setDirection] = useState(1); const [status, setStatus] = useState('Create a match to enter the arena.'); const [winner, setWinner] = useState<string | null>(null); const [selected, setSelected] = useState<number | null>(null); const [started, setStarted] = useState(false);
  const top = discard[discard.length - 1]; const active = players[current]; const canPlay = useMemo(() => active?.hand.map((c) => playable(c, top)) ?? [], [active, top]);

  function startGame(count = 2) { const pile = buildDeck(); const nextPlayers = Array.from({ length: count }, (_, i) => ({ name: i === 0 ? 'YOU' : `RIVAL ${i}`, hand: pile.splice(0, 7), score: 0 })); const first = pile.shift()!; setPlayers(nextPlayers); setDeck(pile); setDiscard([first]); setCurrent(0); setDirection(1); setWinner(null); setSelected(null); setStarted(true); setStatus(`Current: YOU. Match ${count} players. Build the chain.`); }
  function draw() { if (!active || current !== 0 || winner) return; let nextDeck = [...deck]; let nextDiscard = [...discard]; if (!nextDeck.length && nextDiscard.length > 1) { const keep = nextDiscard.pop()!; nextDeck = nextDiscard.sort(() => Math.random() - 0.5); nextDiscard = [keep]; } const card = nextDeck.shift(); if (!card) return; setPlayers(players.map((p, i) => i === current ? { ...p, hand: [...p.hand, card] } : p)); setDeck(nextDeck); setDiscard(nextDiscard); setSelected(null); setStatus('You drew a card.'); }
  function play(card: Card) { if (!active || current !== 0 || !playable(card, top) || winner) return; if (selected !== card.id) { setSelected(card.id); setStatus(`${card.kind} selected — play it again to confirm.`); return; } const nextHand = active.hand.filter((c) => c.id !== card.id); const nextPlayers = players.map((p, i) => i === current ? { ...p, hand: nextHand } : p); setPlayers(nextPlayers); setDiscard([...discard, card]); setSelected(null); if (!nextHand.length) { setWinner(active.name); setStatus(`${active.name} completed the chain.`); return; } let nextDirection = direction; if (card.kind === 'REVERSE CURRENT') nextDirection *= -1; const step = card.kind === 'BREAK' ? 2 : 1; const nextIndex = (current + nextDirection * step + nextPlayers.length) % nextPlayers.length; setDirection(nextDirection); setCurrent(nextIndex); setStatus(card.kind === 'REVERSE CURRENT' ? 'REVERSE CURRENT — the flow has flipped.' : `${card.kind} entered the chain.`); if (nextPlayers.length > 1 && nextIndex !== 0) window.setTimeout(() => aiTurn(nextIndex, nextPlayers, nextDirection, card), 650); }
  function aiTurn(index: number, statePlayers: Player[], dir: number, topCard: Card) { const ai = statePlayers[index]; if (!ai || winner) return; const possible = ai.hand.find((c) => playable(c, topCard)); if (possible) { const hand = ai.hand.filter((c) => c.id !== possible.id); const updated = statePlayers.map((p, i) => i === index ? { ...p, hand } : p); setPlayers(updated); setDiscard((d) => [...d, possible]); let nd = dir; if (possible.kind === 'REVERSE CURRENT') nd *= -1; if (!hand.length) { setWinner(ai.name); setStatus(`${ai.name} completed the chain.`); return; } const skip = possible.kind === 'BREAK' ? 2 : 1; const ni = (index + nd * skip + updated.length) % updated.length; setDirection(nd); setCurrent(ni); setStatus(`${ai.name} played ${possible.kind}.`); if (ni !== 0) window.setTimeout(() => aiTurn(ni, updated, nd, possible), 650); } else { const oldDeck = [...deck]; const drawn = oldDeck.shift(); if (drawn) setPlayers(statePlayers.map((p, i) => i === index ? { ...p, hand: [...p.hand, drawn] } : p)); setDeck(oldDeck); setCurrent(0); setStatus(`${ai.name} drew. Your current.`); } }
  function reset() { setStarted(false); setPlayers([]); setDeck([]); setDiscard([]); setWinner(null); setSelected(null); setStatus('Create a match to enter the arena.'); }

  return <main className="kinetix-app"><header className="kin-nav"><div className="brand"><span className="brand-mark">K</span><div><strong>KINETIX</strong><small>WEB EDITION / 01</small></div></div><div className="nav-center"><span className="pulse" /> LIVE TABLE <b>NO LOGIN</b></div><button className="ghost-btn" onClick={reset}>EXIT MATCH <span>↗</span></button></header>
    {!started ? <section className="landing"><div className="hero-copy"><div className="eyebrow">STRATEGIC CHAIN-BUILDING CARD GAME / 001</div><h1>CONTROL<br /><span>THE CURRENT.</span></h1><p>Every card changes the flow. Link your way forward, break the momentum, reverse the current — and leave the table with nothing in your hand.</p><div className="start-row"><button className="primary-btn" onClick={() => startGame(2)}>ENTER THE ARENA <span>↗</span></button><button className="secondary-btn" onClick={() => startGame(3)}>3 PLAYER <span>+</span></button></div></div><div className="hero-cards"><div className="hero-card-label">THE FIVE FORCES</div><div className="hero-card-stack">{KINDS.map((kind, i) => <div className="float-card" key={kind} style={{ '--i': i } as React.CSSProperties}><CardVisual card={{ id: i, kind, color: COLORS[i % 4], value: kind === 'WILD' ? '✦' : String(i + 1) }} disabled /></div>)}</div></div><div className="landing-grid"><div><b>07</b><span>CARDS / HAND</span></div><div><b>05</b><span>CARD CLASSES</span></div><div><b>100</b><span>CARDS / DECK</span></div><div><b>⟲</b><span>THE CURRENT CAN FLIP</span></div></div></section> : <section className="arena"><div className="arena-top"><div><span className="eyebrow">MATCH / {players.length} PLAYERS / LIVE</span><h2>{winner ? 'CHAIN COMPLETE' : 'THE ARENA'}</h2></div><div className="turn-box"><span>ACTIVE CURRENT</span><strong>{active?.name}</strong><small>{direction === 1 ? 'CLOCKWISE  →' : '←  REVERSED'}</small></div></div><div className="table"><div className="opponents">{players.slice(1).map((p, i) => <div className={`opponent ${current === i + 1 ? 'active' : ''}`} key={p.name}><span className="mini-avatar">0{i + 1}</span><div><b>{p.name}</b><small>{current === i + 1 ? 'THINKING CURRENT' : 'WAITING'}</small></div><strong>{String(p.hand.length).padStart(2, '0')}</strong></div>)}</div><div className="center-pile"><div className="chain-label"><span>CHAIN</span><b>{String(discard.length).padStart(2, '0')}</b></div><div className="pile-cards"><div className="deck-back"><span>K</span><small>KINETIX</small><i>WEB EDITION</i></div>{top && <CardVisual card={top} disabled large />}</div><div className="status-line"><i /> <span>{winner ? `${winner} wins the match.` : status}</span></div></div><div className="your-area"><div className="your-label"><span>YOUR HAND</span><b>{active?.hand.length} CARDS / SCORE {String(active?.score ?? 0).padStart(2, '0')}</b></div><div className="hand">{active?.hand.map((card, index) => <CardVisual key={card.id} card={card} selected={selected === card.id} disabled={current !== 0 || !!winner || !canPlay[index]} onClick={() => play(card)} />)}</div><div className="action-row"><button className="draw-btn" onClick={draw} disabled={current !== 0 || !!winner}>DRAW CARD <span>↓</span></button><span className="hint">{current === 0 ? selected ? 'Click the selected card again to confirm' : 'Playable cards glow · click to select' : 'Opponent is calculating the next current…'}</span></div></div></div><div className="rules-strip"><div><b>01 / LINK</b><span>Color · kind · value</span></div><div><b>02 / BOOST</b><span>Keep momentum</span></div><div><b>03 / BREAK</b><span>Skip the next current</span></div><div><b>04 / REVERSE</b><span>Flip direction instantly</span></div><div><b>05 / WILD</b><span>Connect to anything</span></div></div></section>}
    <footer className="kin-footer"><span>KINETIX / WEB EDITION</span><span>CONTROL THE CURRENT · 2026</span><span>LOCAL PLAY / NO LOGIN</span></footer></main>;
}
